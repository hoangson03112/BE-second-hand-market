name: Warm Render Server

on:
  schedule:
    - cron: "*/8 * * * *"
  workflow_dispatch:

jobs:
  warm:
    runs-on: ubuntu-latest

    steps:
      - name: Ping health endpoint
        run: |
          echo "warming server..."
          curl -s https://be-second-hand-market.onrender.com/health
