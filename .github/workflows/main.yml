name: Keep Render Awake

on:
  schedule:
    - cron: "*/10 * * * *" 
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render API
        run: curl https://be-second-hand-market.onrender.com/eco-market/ping
