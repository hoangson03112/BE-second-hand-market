name: Warm Render Server

on:
  schedule:
    - cron: "*/5 * * * *"

jobs:
  warm:
    runs-on: ubuntu-latest
    steps:
      - name: Warm server
        run: |
          curl -H "User-Agent: Mozilla/5.0" https://api.ecomarket.io.vn/health
