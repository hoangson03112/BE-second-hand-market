name: Keep Render Awake

on:
  schedule:
    - cron: "*/8 * * * *" 
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render API
        run: |
          echo "warming api..."
          curl -s https://be-second-hand-market.onrender.com/eco-market/ping
          curl -s https://be-second-hand-market.onrender.com/eco-market/categories
